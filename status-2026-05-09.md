# HansMed Status Update — 9 May 2026

**Context for the chat:** working alongside another Claude (Cowork mode) that has filesystem + shell access to the Hansmed-system repo. This update covers the 3 days since the last status (`status-2026-05-06.md`), which was the day Google OAuth shipped. Big items since then: full email-verification infrastructure (Resend), Railway region migrated to Singapore, PDPA compliance package drafted, Brief #18 (prescription guidance) Tier 1 data started.

---

## Shipped to production since 6 May

### Auth & navigation fixes (frontend)
- **#14a-fix-12** — Stop session-clobber on `/auth/me` 401. Login persistence across home ↔ portal navigation now stable.
- **#14a-fix-13** — Doctor portal patient detail crashed with `t.points.map is not a function` on records where treatment points were stored as a JSON-stringified array. Added `_parsePoints()` defensive normaliser handling array, JSON-string, CSV, CJK-comma forms.
- **#14a-fix-14** — Logged-in users clicking My Portal/Sign In on the home page were getting bounced to the login modal instead of their role-appropriate portal. Cause: the v3→v2 promotion HTML files never included `nav-auth.js` (the script that wraps `go('login')` for authenticated users). Added the script include to the 4 v3-promoted pages (index, about, services, practitioners).
- **Cache-bust storm** — `?v=14a-fix-12` then `?v=16-email-verify` propagated to api.js, landing.js, components.css, patients.js across 14 HTML files. Cloudflare cache purged manually after each.

### Backend hardening
- **Brief 1A Phase 9** — fully closed (cron service registered, schedule consolidated to 03:00 UTC, ContentLength enforcement, orphan-row cleanup, raw-query scoping). Item 4 (github.io CORS removal) intentionally deferred 2-4 weeks.

### Brief #15 — Google OAuth
- **CONFIRMED LIVE.** Tested with real Gmail; account creation + login working. App is in "Published" mode in Google Cloud Console — no user cap, no 7-day token expiry, no "test users only" restriction.
- New patients via Google: ~5 second signup, no password to forget, email auto-verified.

### Brief #16 — Email infrastructure (this session's main work)
- **16a:** Cloudflare Email Routing for `support@hansmedtcm.com` (and `hello@hansmedtcm.com` if Option B was chosen). Inbound emails forward to `hansmed.moderntcm@gmail.com`.
- **16b:** Gmail Send As alias configured. Required enabling 2FA on the Gmail account first + generating an App Password.
- **16c:** Resend.com signed up via Google OAuth, domain `hansmedtcm.com` verified via Auto-configure DNS records, API key created with **Sending access** scope (more restrictive than Full access).
- **16d:** Laravel `resend/resend-laravel ^0.16` package added; `config/services.php` has `resend.api_key` block; Railway Shared Variables set (`RESEND_API_KEY`, `MAIL_MAILER=resend`, `MAIL_FROM_ADDRESS=support@hansmedtcm.com`, `MAIL_FROM_NAME=HansMed Modern TCM`).
- **16e:** 6-digit email verification on signup. New `email_verification_codes` table (bcrypt'd code, 5-attempt cap, 15-min TTL). Migration also grandfathers existing users by setting `email_verified_at = NOW() WHERE NULL` so nobody got locked out. New endpoints `/auth/verify-email` and `/auth/resend-verification`. Frontend has dedicated verify panel in the auth modal with auto-focus + `autocomplete="one-time-code"` for mobile keyboards.
- **16f:** 6-digit forgot-password (replaces the prior magic-link flow). Same `password_resets` table, just stores bcrypt'd 6-digit code. Frontend has dedicated reset panel. 15-min TTL.
- **Migration applied** on prod DB — `email_verification_codes` table created + grandfathering ran in the same transaction.
- **APP_DEBUG flipped to `false`** (was `true` and leaked SQL/stack traces to users on errors — caught when the migration error briefly exposed a "Table not found" message to the test user).

### v3 → v2 promotion (closing)
- 4 v3 marketing pages (index, about, services, practitioners) live at `/v2/` URLs since 7 May. Old v2 versions preserved as `*-v2-old.html` backups in case rollback needed.

### Brief #18 prep
- Tier 1 TCM reference data spreadsheet generated and saved to `E:\Hansmed-system\reference\tcm-data-tier1.xlsx` (~37 KB). Contains:
  - 30 classical syndromes (證型) with diagnostic criteria
  - 50 classical formulas (方劑) with composition + indications
  - 80 commonly used herbs (藥材) with properties + dose ranges + halal annotations
  - Symptom dictionary (controlled vocabulary mapping patient-spoken variants to standardised terms)
  - 16 red-flag contraindications (pregnancy, drug interactions, paediatric, halal)
- Source: public-domain classical TCM (傷寒論, 金匱要略, 內經, 神農本草經 era texts). The 5th-edition modern textbook PDF the user uploaded is intentionally NOT ingested (copyright). Doc serves as reference only.

### Infrastructure
- **Railway region migrated to Singapore** (`asia-southeast1-eqsg3a`). Confirmed via `railway ssh "echo $RAILWAY_REPLICA_REGION"`. Lower latency for Malaysian users + better PDPA cross-border posture.
- **Resend in Tokyo** (`ap-northeast-1`) — chosen for nearest Asian region available.
- **Cloudflare R2** in APAC region (Asia-Pacific pool — Singapore/Tokyo/Hong Kong).

### PDPA Compliance Package (drafted)
- 30+ KB Word doc at `E:\Hansmed-system\legal\PDPA-Compliance-Package-DRAFT.docx`. 16 sections covering:
  - Decision Points for Counsel (10 specific lawyer questions)
  - Personal Data Inventory + Vendor List
  - Cross-Border Data Transfer disclosures
  - Draft Privacy Policy + Terms of Service
  - Consent texts (PDPA / AI / Marketing / Video) in **English + Bahasa Malaysia + 中文**
  - Personal Data Protection Notice (PDPN, §7-compliant) trilingual
  - Data Subject Rights procedures
  - Breach Response Plan
  - Retention Schedule
  - iPay88 Payment Processor disclosure (added per user's chosen processor)
- Marked clearly as DRAFT, not legal advice. Lawyer engaged for redline. User mentioned "found my lawyer to review" on 9 May.

---

## Open / pending

- **Smoke test Brief #16** — full pipeline (register → receive code → verify → portal; forgot password → receive code → reset). Deferred to morning of 10 May per user.
- **Smoke test Brief #15** — refresh on Google OAuth still working post-#14a-fix-12 cache changes.
- **Lawyer review of PDPA package** — user has lawyer engaged; package is in `/legal/`.
- **Cloudflare cache purge** — purge after every deploy is critical; user has been doing this manually.

---

## Queued (technical, not started)

- **Brief #17 — Doctor onboarding flow** — admin approval workflow for doctor accounts, license number capture, training/orientation. Doctors registering today get `status='pending'` but there's no admin UI to flip them to active.
- **Brief #18 — Prescription guidance system** — Tier 1 data ready in xlsx. Build is "Option A static rule-based, $0 ongoing cost" per earlier decision. Substantial backend + frontend work — 2-4 weeks. Will start once Brief #17 is done OR run in parallel.
- **Brief #19 — iPay88 payment integration** — user committed to iPay88 (Malaysian, supports Touch'n Go eWallet, ShopeePay, GrabPay, FPX, cards). 1-2 week setup including paperwork (SSM, bank account verification with iPay88) + integration code (replace existing Stripe scaffolding which was never put in production). User is in soft-launch with no real payments yet, so safe to swap.
- **Phase 9 Item 4** — Remove `github.io` from R2 CORS allowlist. Deferred 2-4 weeks of stable hansmedtcm.com operation per the brief.
- **Brief #21 (candidate)** — Migrate legacy Railway Volume contents (`/app/storage`) to Cloudflare R2. Currently small (pre-R2 uploads), can be done post-launch.
- **Apple Sign-In** — lower priority than Google for Malaysian market.

---

## Deferred (intentionally)

- **Phase 8 PDPA finalisation** — package drafted, paused in code until lawyer's redline returns.

---

## Stack snapshot

- **Frontend:** static HTML/JS (no build step) on GitHub Pages, fronted by Cloudflare → `hansmedtcm.com`
- **Backend:** Laravel 11 + Sanctum bearer tokens on Railway, **Singapore region** → `hansmed-system-production.up.railway.app`
- **Database:** MySQL on Railway (Singapore)
- **Image storage:** Cloudflare R2 (APAC region) for tongue images, signed PUT/GET with 1hr TTL
- **Transactional email:** Resend (Tokyo) — verification codes + password reset
- **Inbound email:** Cloudflare Email Routing → Gmail for `support@` and `hello@`
- **AI:** Anthropic Claude API (US) for tongue + constitution analysis
- **OAuth:** Google (Apple SSO deferred)
- **Payment:** iPay88 (planned, Malaysian) — Stripe scaffolding will be removed when iPay88 ships
- **Cron:** Railway separate service running `php artisan schedule:run` once daily at 03:00 UTC
- **Domain:** `hansmedtcm.com` (own)
- **Market:** Malaysia, English + Mandarin Chinese (PDPN also in Bahasa Malaysia per §7)

---

## Open business decisions

- **Test patient recruitment plan** — who, how many, what consent (interim form before lawyered-up PDPA)
- **Doctor recruitment + onboarding pipeline** — code blocked on Brief #17, but real-world pipeline (find TCM practitioners, contracts, payouts) is separate
- **Pricing model** — consultation fees, herbal medicine markup, subscription vs pay-per-visit
- **Support staffing** — `support@hansmedtcm.com` is configured but who reads it daily?
- **Marketing copy review** — Mandarin-native eyeball on the v3 marketing pages before launch
- **Backup/restore drill** — confirm Railway Singapore DB snapshot can actually restore (not yet tested)

---

## Today's "watch list" (post-deploy)

- Email deliverability — Resend's reputation is fresh. Verification emails to Gmail may briefly land in Promotions tab in first 1-2 weeks. Advise users to check spam folder + add `support@hansmedtcm.com` to contacts.
- Railway Singapore migration — compute is in Singapore, but DB region migration may need a manual snapshot+restore if the DB itself didn't move. Worth checking.
- Cron service first run — 03:00 UTC = 11 AM Malaysia time. Confirm `tongue:purge-expired-r2` and `tongue:purge-orphans` both fire (logs visible in Railway hansmed-cron service).
