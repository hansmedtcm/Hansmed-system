# Coder Instruction — Rename "AI Diagnosis" → "TCM Wellness Assessment"

**Version:** 1.0
**Date:** 2026-04-21
**Decision authority:** Product owner (HansMed)
**Compliance rationale:** Malaysia MDA 2012 (non-diagnostic framing), PDPA 2010 (accurate notice), Claude Usage Policy (no medical-diagnosis framing for AI output)
**Scope for this task:** Frontend strings only (EN/BM/ZH). Backend API, DB columns, and URLs are **out of scope** for this sprint.

---

## 1. The canonical naming

**Feature name (locked):** `TCM Wellness Assessment`

**Short form / nav label:** `Wellness Assessment`

**Icon tooltip:** `TCM Wellness Assessment`

**Do NOT mention "AI" anywhere in the UI for this feature.** AI involvement is disclosed in Privacy Notice § 5 only. This is a deliberate product decision.

### Trilingual lock

| Language | Full name | Short / nav |
|---|---|---|
| English | TCM Wellness Assessment | Wellness Assessment |
| Bahasa Malaysia | Penilaian Kesihatan TCM | Penilaian Kesihatan |
| 中文 (简体) | 中医健康评估 | 健康评估 |

Use these strings exactly. Do not paraphrase.

---

## 2. Find / replace rules

Apply to frontend only (`**/*.html`, `**/*.js`). Preserve case where possible.

| Find (case-insensitive) | Replace with | Notes |
|---|---|---|
| `AI Diagnosis` | `TCM Wellness Assessment` | Main feature name |
| `AI diagnosis` | `TCM wellness assessment` | Inline body |
| `AI-powered diagnosis` | `TCM wellness assessment` | Drop "AI-powered" |
| `AI-based diagnosis` | `TCM wellness assessment` | Drop "AI-based" |
| `Smart diagnosis` | `TCM wellness assessment` | |
| `Tongue Diagnosis` | `Tongue Assessment` | |
| `Tongue diagnosis` | `Tongue assessment` | |
| `Diagnosis result` | `Assessment result` | |
| `Get diagnosis` | `Get assessment` | CTA |
| `Start AI diagnosis` | `Start TCM Wellness Assessment` | CTA |
| `View diagnosis` | `View assessment` | |
| `Your diagnosis` | `Your wellness assessment` | |
| `AI Assistant` (if pointing to this feature) | `Wellness Assessment` | Review context |

### Do NOT replace these

- `Practitioner diagnosis` — keep if it refers to a licensed practitioner's professional assessment (rare; but check context)
- `Differential diagnosis` — only appears in practitioner-side clinical notes; leave
- `Self-diagnosis` — only appears in warning copy ("do not self-diagnose"); leave

### Manual review required

- `Diagnosis` appearing alone — check context. If referring to AI feature → rename. If medical context warning → leave.
- `Diagnose` (verb) — usually replace with `Assess`.

---

## 3. File targets (from repo survey)

Apply rules above to these paths:

```
index.html
js/chat.js
js/video-consult.js
js/patient-extras.js
js/doctor-wire.js
js/portal-wire.js
js/admin-extras.js
js/checkout.js
js/pos.js
js/tongue-upload.js        (if present)
js/ai-diagnosis.js         (rename file contents; keep filename for now — filename change is out of scope)
services.html              (if present)
```

Any other `**/*.html` or `**/*.js` with matches → include.

---

## 4. Required visual touches

### 4.1 Page header on the feature page

**Before:**
```html
<h1>AI Diagnosis</h1>
<p class="subtitle">Get your diagnosis powered by AI</p>
```

**After:**
```html
<h1 data-i18n="feature.assessment.title">TCM Wellness Assessment</h1>
<p class="subtitle" data-i18n="feature.assessment.subtitle">
  Understand your body through the lens of traditional Chinese medicine.
</p>
```

### 4.2 Required disclaimer (D1 from compliance doc)

Must appear **above** any output block:

```html
<div class="disclaimer disclaimer--assessment" role="note">
  <strong>Important:</strong>
  This wellness assessment is generated by software for educational purposes only.
  It is not a medical diagnosis, does not replace consultation with a registered
  healthcare practitioner, and should not be used to make treatment decisions.
  If you have symptoms that concern you, please consult a qualified practitioner.
</div>
```

Minimum font size 14px. Must not be collapsed by default. Must survive language toggle (BM and ZH versions in the compliance copy doc).

### 4.3 Nav label

**Before:**
```html
<a href="#ai-diagnosis">AI Diagnosis</a>
```

**After:**
```html
<a href="#ai-diagnosis" data-i18n="nav.assessment">Wellness Assessment</a>
```

Note: anchor/URL stays `#ai-diagnosis` for now — URL rename is out of scope.

---

## 5. i18n keys to add

Wire these into the language file (create if absent — even a simple JS object is fine for pilot).

```js
// js/i18n.js (or equivalent)
const translations = {
  en: {
    "feature.assessment.title": "TCM Wellness Assessment",
    "feature.assessment.subtitle": "Understand your body through the lens of traditional Chinese medicine.",
    "feature.assessment.cta.start": "Start Assessment",
    "feature.assessment.cta.view": "View Your Assessment",
    "feature.assessment.disclaimer": "This wellness assessment is generated by software for educational purposes only. It is not a medical diagnosis...",
    "nav.assessment": "Wellness Assessment",
    "tongue.upload.title": "Tongue Wellness Scan",
  },
  bm: {
    "feature.assessment.title": "Penilaian Kesihatan TCM",
    "feature.assessment.subtitle": "Fahami tubuh anda melalui perspektif perubatan tradisional Cina.",
    "feature.assessment.cta.start": "Mulakan Penilaian",
    "feature.assessment.cta.view": "Lihat Penilaian Anda",
    "feature.assessment.disclaimer": "Penilaian kesihatan ini dijana oleh perisian untuk tujuan pendidikan sahaja. Ia bukan diagnosis perubatan...",
    "nav.assessment": "Penilaian Kesihatan",
    "tongue.upload.title": "Imbasan Kesihatan Lidah",
  },
  zh: {
    "feature.assessment.title": "中医健康评估",
    "feature.assessment.subtitle": "通过中医的视角了解您的身体。",
    "feature.assessment.cta.start": "开始评估",
    "feature.assessment.cta.view": "查看您的评估",
    "feature.assessment.disclaimer": "此健康评估由软件生成，仅供教育参考。它不是医疗诊断，不能替代注册医疗从业者的咨询...",
    "nav.assessment": "健康评估",
    "tongue.upload.title": "舌象健康扫描",
  }
};
```

Full BM and ZH disclaimer text comes from `docs/ux/compliance-copy-changes.md` D1 section (expand there if only a snippet is in this doc).

---

## 6. Backend — minimal touch only

Backend is **out of scope** for this task, but:

- When you encounter API response `message` fields with "diagnosis" inline, note the file/line in a `BACKEND_RENAME_TODO.md` at repo root. Do not change in this pass.
- Database columns (`ai_diagnoses` table, if it exists) — do NOT rename now. Requires migration + historical data consideration. Add to `BACKEND_RENAME_TODO.md`.

---

## 7. Acceptance criteria

Before marking this task done, verify:

- [ ] `grep -rE "AI[- ]?[Dd]iagnosis" ` returns zero hits
- [ ] `grep -rE "Tongue [Dd]iagnosis" ` returns zero hits
- [ ] Feature page hero reads "TCM Wellness Assessment"
- [ ] Nav label reads "Wellness Assessment"
- [ ] Disclaimer renders above output, font ≥ 14px
- [ ] Language toggle switches between EN / BM / 中文 and all three show the locked strings above
- [ ] CTA buttons read "Start Assessment" / "View Your Assessment" (or locale equivalents)
- [ ] No mention of "AI" anywhere in the feature page, nav, or CTAs
- [ ] `BACKEND_RENAME_TODO.md` created at repo root with every backend occurrence noted (file:line)
- [ ] Manual smoke test: walk the full flow (home → Wellness Assessment → tongue upload → result page → share with practitioner) in all 3 languages

---

## 8. Scope boundaries — do NOT do these

- ❌ Do not rename backend routes, controllers, or DB columns
- ❌ Do not change URL slugs (`#ai-diagnosis` stays for now)
- ❌ Do not rewrite marketing site / landing page copy (separate task)
- ❌ Do not touch email templates (separate task)
- ❌ Do not restyle the feature — only copy and the new disclaimer

Keep the diff focused so it can be reviewed quickly.

---

## 9. Handoff back to coordinating Claude

After you finish:
1. Commit with message: `chore(copy): rename AI Diagnosis → TCM Wellness Assessment (frontend only)`
2. Produce a short diff summary (files touched, lines changed)
3. Confirm the `BACKEND_RENAME_TODO.md` file is at repo root

Coordinating Claude will then update the pilot readiness checklist and schedule the backend rename for the next sprint.
