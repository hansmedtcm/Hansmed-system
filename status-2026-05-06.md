# HansMed Status Update — 6 May 2026

**Context for the chat:** I've been working with another Claude (Cowork mode) for ~10 hours straight on the platform. Here's what shipped, what's pending, and what's queued.

---

## Shipped to production today

### Frontend (GitHub Pages)

- **Brief #14a-fix-7** — Patient AI Review list hides the duplicate tongue card when paired with a constitution submission. The constitution card already inlines the tongue analysis, so showing both was confusing. Standalone cards (tongue-only or constitution-only) unaffected.
- **Brief #14a-fix-8** — Paired sessions now show "👅🧭 Tongue + Constitution · 舌診 + 體質" instead of just "Constitution".
- **Brief #14a-fix-9** — Auto-popup login modal now fires only on FRESH entries (direct URL, external referrer, new tab). Internal navigation and refresh skip it. No more "tap a widget, get a login screen" feeling on mobile.
- **v3 → v2 promotion** — Redesigned marketing pages (index/about/services/practitioners) now serve at `/v2/` URLs. Old pages preserved as `*-v2-old.html` for rollback.

### Backend (Laravel 11 on Railway)

- **Phase 9 Item 2** — ContentLength enforcement on R2 uploads (rejects oversized PUTs after the fact).
- **Phase 9 Item 3** — `tongue:purge-orphans` artisan command for stuck `r2://pending` rows >24h old.
- **Phase 9 Item 5** — `whereNull('deleted_at')` added to 5 raw queries in `Admin/MigrationController` for consistency.
- **Phase 9 Item 1** — Railway cron service `hansmed-cron` registered at `0 3 * * *` UTC, runs `php artisan schedule:run`. Both purge commands consolidated to fire at 03:00 in a single invocation. ~$0/month cost.

---

## In flight (code shipped, awaiting deploy + config)

### Brief #15 — Google OAuth login

- **Backend:** pushed (commit `740cdff`). Includes `laravel/socialite` dependency, `users.google_id` migration (already run on prod DB), `AuthGoogleController` with redirect/callback/exchange endpoints using a 60-second single-use exchange-code pattern (token never appears in URL), 3 routes, services + app config.
- **Frontend:** code staged locally (Continue-with-Google button on login + register tabs, `#/google-exchange` route handler in `landing.js` that POSTs the code, stores token via existing `api.setToken/setUser`, redirects to `portal.html`). NOT yet pushed.
- **Required before live:** 4 Railway Shared Variables (`GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI`, `FRONTEND_URL`). I have the Client ID/Secret saved; just need to paste them into Railway.

---

## Queued (Brief #16)

- `hello@hansmedtcm.com` email mailbox via Cloudflare Email Routing (free, forwards to my Gmail; I send via Gmail "send as" alias)
- Resend.com for transactional email (free tier 100/day → 3000/month, covers entire soft-launch)
- 6-digit code email verification on signup
- 6-digit code forgot-password flow
- DNS records (SPF, DKIM, DMARC) on hansmedtcm.com for deliverability

---

## Deferred (intentionally)

- **Phase 8 PDPA** — consent UI scaffold drafted, but legally-hardened content is paused until lawyer review. Don't need it for soft-launch (no real customers yet).
- **Phase 9 Item 4** — Remove `github.io` from R2 CORS allowlist. Wait 2-4 weeks of stable hansmedtcm.com operation first.
- **Doctor onboarding flow** — separate brief, post-launch.
- **Apple Sign-In** — lower priority than Google in my market.

---

## Stack snapshot

- **Frontend:** Static HTML/JS (no build step) on GitHub Pages, fronted by Cloudflare → `hansmedtcm.com`
- **Backend:** Laravel 11 + Sanctum bearer-token auth on Railway → `hansmed-system-production.up.railway.app`
- **Storage:** Cloudflare R2 (S3-compatible) for tongue images, signed PUT/GET with 1hr TTL
- **DB:** MySQL on Railway, ~5 services in the project
- **Domain:** `hansmedtcm.com` (own), considering `hansmed.com` later for branding
- **Market:** Malaysia, Mandarin + English bilingual

---

## Open business decisions (not technical)

- Test patient recruitment plan for soft-launch
- Doctor onboarding UX (how a TCM practitioner gets approved + paid)
- Support inbox routing (where `hello@`, `support@`, etc. emails go)
- Pricing + payment flow (Stripe + FPX for Malaysia? still mapping)
